# Instructions

## Follow this steps for set up local environment

In the root project run :
- `npm i`
- `docker-compose up`  (This is for database so far)
- `npm run start-dev`

##Endpoints:

- `/parse` `POST`